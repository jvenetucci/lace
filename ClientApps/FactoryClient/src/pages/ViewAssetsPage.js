import React from 'react'

// This is where a local DB that tracks which items you are currently holding. 
// Currently not implemented in this branch.
const ViewAssetsPage = () => (
  <div>
    Assets in inventory.
  </div>
)

export default ViewAssetsPage