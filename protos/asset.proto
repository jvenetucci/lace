syntax = "proto3";


message Asset {

  // A unique string used to calculate the
  // address.
  optional string rfid = 1;

  // The sizes or each shoe.
  optional float size_r = 2;
  optional float size_l = 3;

  // Barcode?
  optional string SKU = 4;
}
